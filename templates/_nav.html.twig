<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm mb-4">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('app_home') }}">Planificateur de Dons</a>
                
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            {# LIEN VERS LA VUE BÉNÉFICIAIRES #}
                            <a class="nav-link" href="{{ path('app_simulation_planning_donataires') }}">Vue donataires (qui reçoivent)</a>
                        </li>
                        <li class="nav-item">
                            {# LIEN VERS LA VUE DONATEURS #}
                            <a class="nav-link" href="{{ path('app_simulation_planning_donateurs') }}">Vue donateurs (qui donnent)</a>
                        </li>
                    </ul>
                    
                    {# Vous pouvez ajouter ici des boutons de connexion/déconnexion ou un formulaire de recherche si nécessaire #}
                    {% if app.user %}
                        <ul class="navbar-nav">
                            <li class="nav-item">
                                <a class="nav-link btn btn-sm btn-outline-light ms-2" href="{{ path('app_logout') }}">Déconnexion</a>
                            </li>
                        </ul>
                    {% endif %}
                </div>
            </div>
        </nav>