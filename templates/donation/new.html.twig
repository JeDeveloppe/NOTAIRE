{# On ne met PAS de extends 'base.html.twig' ici #}

<div class="donation-form-container">
    {# On peut ajouter un petit titre interne si nécessaire, mais le header de la modale suffit généralement #}
    {{ include('donation/_form.html.twig') }}
</div>

<style>
    /* Optionnel : Ajustements spécifiques pour l'affichage en modale */
    .donation-form-container .card {
        border: none !important;
        box-shadow: none !important;
        padding: 0 !important;
    }
</style>