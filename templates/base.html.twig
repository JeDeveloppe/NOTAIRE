<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<title>
			{% block title %}Lignée - Optimisez votre patrimoine familial{% endblock %}
		</title>

		{# --- FAVICONS & PWA --- #}
		{# La fonction asset() pointe vers assets/images/favicon/ et gère le versioning #}
		{# Remplace ton bloc FAVICONS par celui-ci, plus robuste #}
		<link rel="icon" type="image/svg+xml" href="{{ asset('images/favicon/favicon.svg') }}"> <link rel="icon" type="image/png" href="{{ asset('images/favicon/favicon-96x96.png') }}" sizes="96x96">
		<link rel="apple-touch-icon" sizes="180x180" href="{{ asset('images/favicon/apple-touch-icon.png') }}">
		<link rel="shortcut icon" href="{{ asset('images/favicon/favicon.ico') }}">
		<meta name="apple-mobile-web-app-title" content="Lignée"/>
		<linkrel="manifest"
		href="{{ asset('images/favicon/site.webmanifest') }}"/>

		{# --- RESSOURCES EXTERNES --- #}
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">

		{% block stylesheets %}
			{# Vos styles personnalisés compilés via AssetMapper #}
		{% endblock %}

		{% block javascripts %}
			{% block importmap %}
				{{ importmap('app') }}
			{% endblock %}
		{% endblock %}
	</head>

	<body
		class="d-flex flex-column min-vh-100 bg-light">

		{# Inclusion des messages Flash (Succès, Erreurs) #}
		{% include 'components/site/_flash_message.html.twig' %}

		{# Barre de navigation avec ton logo Arbre #}
		{% include 'components/site/_navigation.html.twig' %}

		{# Contenu principal #}
		<main class="flex-shrink-0 py-5">
			<div class="container">
            {% block body %}{% endblock %}
			</div>
		</main>

		{# Pied de page #}
		{% include 'components/site/_footer.html.twig' %}

	</body>
</html>